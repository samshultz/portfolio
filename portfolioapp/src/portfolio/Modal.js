import React from 'react';


import ModalInline from './ModalInline';

const Modal = (props) => {
    
        return (
            <ModalInline project={props.project}/>

        );
    }

export default Modal;
